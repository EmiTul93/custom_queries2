spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: EmisTul.93
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
